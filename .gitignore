# C build artefacts
*.o
*.so
*.a
*.lo
*.la

# macOS metadata
.DS_Store

# paper-index hook artifacts (local navigation aids; not project content)
.paper-index.json
**/.paper-index.json

# Project-internal governance — kept local, not tracked publicly
CLAUDE.md

# Project-internal governance / process records — kept local, not tracked publicly
PHAROS_SPEC.md
artifact_registry.md
changelog.md
docs/
dashboard.md

# CI / build outputs
build/
target/

# macOS Authorization Plug-in build outputs (source-of-truth lives
# in LavaLampMechanism.m + Makefile + Info.plist; rebuilt by `make`)
src/macos/LavaLampMechanism.bundle/Contents/MacOS/
src/macos/LavaLampMechanism.bundle/Contents/_CodeSignature/

# Lean build artifacts
src/lean4/.lake/

# Python virtualenvs (PH-019 macos-reactor dev iteration)
.venv/
**/.venv/
__pycache__/
**/__pycache__/
*.pyc

# Notarized bundle stapled ticket (Apple-issued, per-build)
src/macos/LavaLampMechanism.bundle/Contents/CodeResources

# Notarytool input artefacts (regenerated each release)
src/macos/LavaLampMechanism.bundle.zip
src/macos/LavaLampMechanism.bundle.zip.log

# Local overrides for signing identity (gitignored Makefile.local pattern)
src/macos/Makefile.local

# Whitepaper PDF build output (LaTeX source is canonical)
lavalamp_whitepaper.pdf
